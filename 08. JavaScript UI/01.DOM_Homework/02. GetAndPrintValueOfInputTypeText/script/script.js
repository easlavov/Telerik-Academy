/* Create a function that gets the value of <input
type="text"> ands prints its value to the console */

var changeColorButton = document.getElementsByTagName('button')[0];
changeColorButton.addEventListener('click', extractText, false);